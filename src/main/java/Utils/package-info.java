/**
 * Contains some function and utilities that are used in the whole applications. That allows to centralize, standardize
 * some repetitive functions, and often eliminate the need to make duplicate code.
 */
package Utils;